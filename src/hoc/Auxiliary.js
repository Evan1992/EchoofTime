/**
 * 
 * @note hoc represent for higher order component
 * @brief just a javascript function, don't have jsx in this file, so no need to import React 
 */

const Aux = (props) => props.children

export default aux